---
summary: "Shared models used across calendar, LLM, and messaging"
read_when:
  - "When changing shared data structures"
---

# Shared Package

Defines the shared data model passed between calendar, LLM, and messenger layers.

- `CalendarEvent` dataclass fields: `title`, `start`, `end`, `all_day`, `description`, `location`.
